const URL_BACKEND = 'https://localhost:5001/'; //LOCAL

export const ENVIRONMENT = {
    URL_BACKEND: URL_BACKEND,

    // Urls Apis
    URL_API_CLIENTS: URL_BACKEND + 'api/Clients',
    URL_API_PRODUCTS: URL_BACKEND + 'api/Products',
};
